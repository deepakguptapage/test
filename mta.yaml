_schema-version: "3.2"
ID: ui50001
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ui50001-destination-content
  type: com.sap.application.content
  requires:
  - name: ui50001-destination-service
    parameters:
      content-target: true
  - name: ui50001-repo-host
    parameters:
      service-key:
        name: ui50001-repo-host-key
  - name: ui50001-uaa
    parameters:
      service-key:
        name: ui50001-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ui50001_html_repo_host
          ServiceInstanceName: ui50001-html5-srv
          ServiceKeyName: ui50001-repo-host-key
          sap.cloud.service: ui50001
        - Authentication: OAuth2UserTokenExchange
          Name: ui50001_uaa
          ServiceInstanceName: ui50001-xsuaa-srv
          ServiceKeyName: ui50001-uaa-key
          sap.cloud.service: ui50001
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ui50001-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ui50001-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ui50001.zip
      name: ui50001
      target-path: resources/
- name: ui50001
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ui50001-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ui50001-destination-service
    service-plan: lite
- name: ui50001-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ui50001-xsuaa-srv
    service-plan: application
- name: ui50001-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ui50001-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
